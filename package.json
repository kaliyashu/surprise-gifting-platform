{
  "name": "surprise-moments-platform",
  "version": "1.0.0",
  "description": "A web-based platform for creating and sharing personalized surprise experiences",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "start": "cd server && npm start"
  },
  "keywords": ["surprise", "gifting", "animations", "personalized", "web-app"],
  "author": "Surprise Moments Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
